name: Release

on:
  workflow_dispatch:
  create:


jobs:
  tag:
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
    steps:

    # - uses: actions/checkout@v2

    # - name: Setup Python
    #   uses: actions/setup-python@v2
    #   with:
    #     python-version: ${{ matrix.python }}

    - run: echo 'created tag'
